# Integriscan Web

This folder contains the React Frontend Project

## Getting Started

1. Install dependencies:
   `npm install`

2. Update the VITE_API_BASE_URL in .env.example then rename the file to .env.local

3. Run the app:
   `npm run dev`

