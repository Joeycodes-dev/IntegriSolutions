# Web app → backend API
VITE_API_BASE_URL=http://localhost:4000

# Public origin used in court verification QR codes (e.g. https://portal.example.com).
# Required for production so scanned QR links resolve to the hosted dashboard.
VITE_PUBLIC_WEB_URL=http://localhost:3000
