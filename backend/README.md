# Integriscan Server

This folder contains the Backend for the Integriscan backend.

## Getting Started

1. Install dependencies:
   `npm install`

2. Update the Supabase configuration info in .env.example then rename the file to .env.local

3. Run the app:
   `npm run dev`
